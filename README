John James 811-597-859

To compile the program, use the "$make" command. To run the program, use the "$make run" command. To clean the program, use the "$make clean" command.

This project was not done in a group.